{
	"name": "@chientrm/colyseus-not-sync-game",
	"version": "1.0.0",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"build": "npm run clean && node build.js && npx tsc -p tsconfig.json",
		"watch": "node watch.js",
		"clean": "rimraf dist",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@colyseus/schema": "^2.0.25",
		"esbuild": "^0.19.8"
	}
}
